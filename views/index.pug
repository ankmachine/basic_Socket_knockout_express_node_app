extends layout

block content
  h1= title
  
  
  form
    span username
    input(data-bind='value: username')
    button(data-bind='click: enterChat') enter
  
  .col-md-6
    #future
    form
      select(data-bind='options: roomList, value: selectedRoom' )
      div.ol(data-bind='foreach: chatList')
        li.span(data-bind='text: $data')
      input(data-bind='value: chatBody')
      button(data-bind='click: submit') submit
  .col-md-3
    h3 notification
    div.ol(data-bind="foreach: eventCounter")
      ul
        span(data-bind='text: name')
        span.badge(data-bind='text: number')
  .col-md-3
    h3 Live
    div.ol(data-bind='foreach: notificationList')
        li.span(data-bind='text: $data')
        
  

block PostScripts
  script(src='https://cdnjs.cloudflare.com/ajax/libs/knockout/3.4.2/knockout-min.js')
  script(src='https://cdnjs.cloudflare.com/ajax/libs/knockout.mapping/2.4.1/knockout.mapping.js')
  script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.js')
  script(src='https://cdnjs.cloudflare.com/ajax/libs/socket.io/1.7.3/socket.io.js')
  //- script(src='https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.0.0-alpha.6/css/bootstrap-grid.css');
  script(src='/javascripts/index.js')
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">